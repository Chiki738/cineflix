# CineFlix

Aplicación React + Vite para explorar películas, series, listas de usuario, historial y panel de administración.

## Comandos

```bash
pnpm install
pnpm run dev
pnpm run build
pnpm run lint
pnpm audit
```
